# Project Uji Coba

Simple landing page starter for quick demos.

Files:

- `index.html` — main landing page
- `styles.css` — page styles
- `script.js` — small interactivity

To preview locally, open `index.html` in your browser (double-click or use a local server).

Customize the content, colors, and scripts as needed.
